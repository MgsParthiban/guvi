services:
  webapp:
    image: "${prod_image}"
    ports: 
    - 1111:80
    networks:
      - guvi
    secrets:
      - docker_credentials
networks:
  guvi:
secrets:
  docker_credentials:
    file: ./secret.txt
